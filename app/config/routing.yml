backend:
    resource: "@BackendBundle/Controller/"
    type:     annotation
    prefix:   /admin

frontend:
    resource: "@FrontendBundle/Controller/"
    type:     annotation
    prefix:   /

app:
    resource: '@AppBundle/Controller/'
    type: annotation
    prefix:   /app
