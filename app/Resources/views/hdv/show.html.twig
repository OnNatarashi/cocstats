{% extends 'base.html.twig' %}

{% block body %}
    <h1>Hdv</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hdv.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ hdv.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('hdv_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('hdv_edit', { 'id': hdv.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
